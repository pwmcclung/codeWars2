//code walkthrough
// this line initializes the function with one parameter, names
sortme = function( names ){
    //this line returns names after using the sort array method
    //the sort() method sorts the elements in place and returns the sorted array
    return names.sort()
  }